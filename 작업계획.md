# BYREDO 웹 퍼블리싱 → React(Vite) 변환 작업 계획서

## 1. 프로젝트 개요
- **프로젝트 경로**: `D:\황인영\byredo`
- **원본 위치**: `org/` (HTML, CSS, JS 및 `img` 폴더 포함)
- **프레임워크**: React (Vite)
- **언어**: TypeScript
- **라우팅**: `/` (단일 메인 페이지 기준)
- **스타일링**: Styled Components

## 2. 디자인 및 반응형 유지 전략 (최우선 원칙)
> [!IMPORTANT]
> **기존 디자인 유지**: 현재 `org/`에 구현된 퍼블리싱 결과물을 시각적으로 100% 동일하게 유지합니다. 
> 레이아웃, 화면 구조, 위치, 크기, 여백, 폰트, 색상, 이미지, 아이콘, 버튼, border, border-radius, 애니메이션 등 모든 요소를 임의로 변경·개선하지 않습니다.

### 반응형 동작 및 브레이크포인트 분석
현재 `org/style.css`에 적용된 모바일 퍼스트 반응형을 그대로 유지합니다.
- **모바일 (480px 이하)**: 
  - 단일 컬럼 레이아웃 (`app-frame`이 화면 전체 차지).
  - 전체 페이지 헤더(`site-header`) 숨김 처리, 하단 탭바 노출 등 모바일 전용 뷰 제공.
- **데스크탑 (481px 이상 - `@media (min-width: 481px)`)**: 
  - 좌측 `app-column` (60%) / 우측 `brand-column` (40%)의 2단 레이아웃 분기.
  - 상단 `site-header` 노출 및 고정.
- **와이드 데스크탑 (1280px 이상 - `@media (min-width: 1280px)`)**:
  - 컨테이너 폭이 특정 크기(1180px)로 고정되고 중앙 정렬됨.

## 3. 이미지 에셋 처리 전략
사용자 요구사항에 따라 `org/img` 폴더를 **절대 삭제하거나 이동하지 않고** 그대로 유지합니다.
- Vite 정적 자산 처리 및 절대 경로 연동 혹은, import 구문(`import logo from '../../../org/img/logo.png'`)을 통해 원본 이미지를 그대로 호출합니다.
- 변환 시 기존 CSS의 비율, 표시 방식(width, height, object-fit 등)을 1:1로 매핑하여 이미지 깨짐을 원천 차단합니다.

## 4. 폴더 및 파일 구조 계획
거대한 단일 Styled-Component 파일을 지양하고, 페이지 및 컴포넌트 구조에 맞게 파일을 세분화하여 관리합니다.

- `src/`
  - `components/`
    - `common/`
      - `Header.tsx`, `Header.styles.ts` : 상단 공통 헤더
      - `Footer.tsx`, `Footer.styles.ts` : 하단 공통 푸터
    - `home/` (메인 페이지 전용)
      - `HeroBanner.tsx`, `HeroBanner.styles.ts`
      - `CategoryGrid.tsx`, `CategoryGrid.styles.ts`
      - `LiveSearchRank.tsx`, `LiveSearchRank.styles.ts`
      - `BestProducts.tsx`, `BestProducts.styles.ts`
    - `layout/` 
      - `AppLayout.tsx`, `AppLayout.styles.ts` (`app-column` & `brand-column` 2단 레이아웃 처리)
  - `pages/`
    - `Home.tsx` : `/` 라우터 기준 메인 조립 컴포넌트
  - `styles/`
    - `GlobalStyle.tsx` : `createGlobalStyle`을 통한 공통 폰트/리셋 CSS/CSS 변수 적용

## 5. CSS → Styled Components 변환 규칙
- **시각적 결과 100% 보존**: 기존 CSS의 font, color, width, height, margin, padding, gap, display, position, border, shadow, transition 속성 및 media query를 누락 없이 1:1로 이관합니다.
- **컴포넌트 단위 스타일링**: `style.css`의 내용 전체를 하나의 파일로 만들지 않고, 각각의 컴포넌트(`*.styles.ts`)로 적절히 쪼개어 관리합니다.

## 6. JavaScript 로직 → React State 변환 전략
- `org/script.js`의 바닐라 JS 이벤트를 React State(`useState`, `useEffect`)로 1:1 변환합니다.
- 캐러셀 인덱스 제어, 탭/모달 팝업 및 햄버거 메뉴의 열림/닫힘 상태, 스크롤 이벤트를 관리하여 기존과 동일한 사용자 경험을 제공합니다.
